## 1. Auto-Focus for Primary Action Modals

- [x] 1.1 Add `useRef` auto-focus to primary action button ("Next Round" / "Retry Round") in `RoundSummaryModal`
- [x] 1.2 Add `useRef` auto-focus to primary confirmation button in `ResetConfirmationModal`
- [x] 1.3 Add `useRef` auto-focus to primary action button in `CampaignEndModal`
- [x] 1.4 Add `useRef` auto-focus to primary action button ("Start Campaign") in `CampaignSetupModal`

## 2. Auto-Focus for Informational & Secondary Modals

- [x] 2.1 Add `useRef` auto-focus to Close button in `RulesModal`
- [x] 2.2 Add `useRef` auto-focus to Close button in `KeyboardShortcutsModal` and `MatchedCardsModal`
- [x] 2.3 Ensure `Escape` key listeners reliably close open modals without triggering active gameplay shortcuts

## 3. Verification & Testing

- [x] 3.1 Verify auto-focus behavior and keyboard interaction across all modals via tests and build verification
