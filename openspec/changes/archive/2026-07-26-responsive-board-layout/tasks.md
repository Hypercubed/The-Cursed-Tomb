## 1. Shell & Container Sizing

- [x] 1.1 Update `GameShell.tsx` container max-width to support fluid high-resolution displays (up to 1600px)
- [x] 1.2 Optimize mobile padding and flex grid layout across breakpoints in `GameShell.tsx` and `App.tsx`

## 2. Responsive Card & Slot Scaling

- [x] 2.1 Update `PlayingCard.tsx` base width, height, padding, rank typography, and suit SVG sizes across screen breakpoints
- [x] 2.2 Update `DrawZone.tsx` draw pile and discard pile slots to match responsive card dimensions

## 3. Pyramid Overlap & Board Layout

- [x] 3.1 Update `PyramidBoard.tsx` negative top margin row overlap classes to scale proportionally with card height
- [x] 3.2 Verify layout build and test suite execution
