## 1. Core Component Updates

- [x] 1.1 Update `SuitIcon` in `src/components/PlayingCard.tsx` to use standard vector SVG paths for Hearts (♥), Diamonds (♦), Spades (♠), and Clubs (♣).

## 2. Verification & Testing

- [x] 2.1 Verify card center suit icons render correctly across viewports.
- [x] 2.2 Run test suite and build verification (`npm test`, `npm run build`).
