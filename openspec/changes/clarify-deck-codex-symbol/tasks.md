## 1. Update Deck Codex Legend

- [ ] 1.1 In `src/components/MatchedCardsModal.tsx`, replace the legend span text `|#\|` with `|N\|`

## 2. Update Spec

- [ ] 2.1 In `openspec/specs/card-rendering/spec.md`, replace both occurrences of `|#\|` with `|N\|` in the "Attrition Scar Tooltips and Legends" requirement and its scenario
